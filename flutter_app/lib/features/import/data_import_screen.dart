import 'package:flutter/material.dart';

import '../../shared/widgets/placeholder_scaffold.dart';

class DataImportScreen extends StatelessWidget {
  const DataImportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScaffold(
      title: 'Import Data',
      subtitle: 'Phase 6 placeholder: onboarding import and success summary flow land here.',
    );
  }
}
