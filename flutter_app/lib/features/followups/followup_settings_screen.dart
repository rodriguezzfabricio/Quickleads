import 'package:flutter/material.dart';

import '../../shared/widgets/placeholder_scaffold.dart';

class FollowupSettingsScreen extends StatelessWidget {
  const FollowupSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScaffold(
      title: 'Follow-up Settings',
      subtitle: 'Phase 3 placeholder: sequence toggles, channels, and template editing land here.',
    );
  }
}
