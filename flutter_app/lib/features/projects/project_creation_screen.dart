import 'package:flutter/material.dart';

import '../../shared/widgets/placeholder_scaffold.dart';

class ProjectCreationScreen extends StatelessWidget {
  const ProjectCreationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScaffold(
      title: 'New Project',
      subtitle: 'Phase 4 placeholder: lead linking and project setup inputs land here.',
    );
  }
}
